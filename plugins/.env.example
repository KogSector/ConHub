# Plugins Service Environment Variables
# Copy this file to .env and fill in your actual values

# === Service ===
PLUGINS_SERVICE_PORT=3020
ENV_MODE=development

# Path to plugin configuration JSON
# Defaults to ./config/plugins.json if unset
PLUGIN_CONFIG_PATH=./config/plugins.json

# === Logging ===
# Set the log level (error, warn, info, debug, trace)
RUST_LOG=info

# Notes:
# - The service will auto-create a default plugins.json if it doesn’t exist
# - No database configuration is required for the Plugins service
# - Never commit .env files to version control